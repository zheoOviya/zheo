// Sprint 1 (I-07): add-to-cart feedback timing.
// PROCESSING is the minimum time the confirm button shows a spinner (so the
// transition is visible), SUCCESS is the green checkmark dwell before the
// picker closes. Exported for deterministic tests.
export const ADD_PROCESSING_MS = 400;
export const ADD_SUCCESS_MS = 300;
